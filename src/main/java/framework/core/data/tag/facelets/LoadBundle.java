package framework.core.data.tag.facelets;

public class LoadBundle
{
	public String basename;// The resource bundle name
	public String value;// The name of the variable that is bound to the bundle
						// map
}
